name: ROS 2 CI

on:
  push:
  pull_request:

jobs:
  build_and_test_ros2:
    runs-on: ubuntu-22.04
    steps:
      - uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: humble
      - uses: ros-tooling/action-ros-ci@v0.4
        with:
          package-name: lds_lidar
          target-ros2-distro: humble
